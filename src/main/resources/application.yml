spring:
  jpa:
    hibernate:
      show-sql: true
      #      TODO : ?? ??? ? ??? ?? show-sql true? ?? ?? ?.
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PWD}